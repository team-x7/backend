name: CICD pipeline

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

env:
  IMAGE_NAME: ramankarki/x7-backend

jobs:
  push:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read

    steps:
      - uses: actions/checkout@v3

      - name: Build image
        run: docker image build --tag $IMAGE_NAME:$GITHUB_RUN_ID --target prod .

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Push image
        run: |
          docker push $IMAGE_NAME:$GITHUB_RUN_ID

      - name: Checking
        run: |
          echo ${{ secrets.DIGITALOCEAN_USERNAME }}
          echo ${{ secrets.HOST }}
          echo ${{ secrets.SSH_PORT }}
          echo ${{ secrets.SSH_PRIVATE }}
          echo ${{ secrets.SSH_PHRASE }}

      - name: executing remote ssh commands using ssh key
        uses: appleboy/ssh-action@v0.1.7
        with:
          username: ${{ secrets.DIGITALOCEAN_USERNAME }}
          host: ${{ secrets.HOST }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_PRIVATE }}
          passphrase: ${{ secrets.SSH_PHRASE }}
          script: |
            whoami
            docker service ls
